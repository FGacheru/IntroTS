const superHeros: string[] = [""]
const heroPower: Array<number> = []

superHeros.push("")
heroPower.push(4)

type User = {
    name: string;
    isActive: boolean;
}

const allUsers: User[] = []

allUsers.push({name: "Gacheru", isActive: true})

const MlModels: number [][] = [
    [255, 255, 255],
    []
]


//ARRAY

const data: number[] = [1, 2, 3];
const data2: string[] = ["1", "2", "3"];
const data3: (string | number | boolean)[] = ["", 2, true];

let seatAllotment: "aisle" | "middle" | "window";

seatAllotment = "aisle";
// seatAllotment = "crew"


export {}